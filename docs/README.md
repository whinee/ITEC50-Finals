# DeciMark

## License

See [LICENSE.md](./LICENSE.md) for more details. Please read carefully as the project is dual-licensed. If in doubt, do not hesitate to contact me and inquire about licensing.

## Initial setup

Requires [direnv](https://direnv.net/).

```bash
mkdir -p ~/.config/direnv
# Either copy or append the layout script
cat .direnv.uv >> ~/.config/direnv/direnvrc
direnv allow
```

```sh
pipx install latexminted
```

## What I Had to Run

```sh
uv venv
source .venv
```

```sh
just start-db
```

```sh
just create-db
```

Run the following command if you need to re-initialize `src/migrations`:

```sh
just alembic init src/migrations
```

```sh
just alembic revision --autogenerate -m "init-commit"
```

```sh
just alembic upgrade head
```

## AI Disclosure

In the interest of academic integrity, the following portions of this project were generated or heavily modified with the assistance of artificial intelligence tools:

- **HTML Templates**: All HTML files under the `src/templates/bookmarks` directory were generated by OpenAI Codex.
- **Backend Scripts**: The majority of the Python code within the `src/scripts` directory was generated by Anthropic's Claude AI.
- **Recent Backend & Frontend Modifications**: Google DeepMind's Antigravity assistant generated and modified the following components:
  - The `edit` bookmark functionality and updates to `src/api/bookmarks.py` and `src/static/scripts/bookmarks.js`.
  - Authentication tightening in `main.py` and the `/login` auto-redirect endpoint.
  - Hamburger menu styling and the SVG CSS `mask-image` icon theming technique in `base.css` and `base.j2.html`.
  - Comprehensive CSS accessibility fixes to replace hardcoded colors with theme variables across `style.css`, `bookmarks.css`, `code.css`, and `base.css`.
  - Removal of deprecated login templates and CSS redundancies.
  - Structural unification and completion of CSS theme variables for dark and light modes in `base.css`.
  - Refactoring of frontend HTML forms and backend API payloads to support multiple JD IDs per bookmark.
  - The memory-safe, bulk-insert rewrite of the `scripts/seed.py` generation pipeline to scale up to millions of rows while enforcing chronological datetime constraints.
  - The creation of a stunning new glassmorphic landing page (`index.j2.html`).
  - The addition of the `GET /` route in `main.py` to securely expose the total database bookmark count without data leakage.
  - The authorship of the Johnny.Decimal justification section in the LaTeX rationale.
  - The exhaustive sub-sectioning of the entire CSS asset directory with start/end comment delimiters to facilitate programmatic tree generation and markdown summaries.
  - The implementation of a robust Pytest and Playwright automated UI test suite with `scripts/run_visual_tests.py` and `tests/e2e/test_visual_and_lighthouse.py`.
  - Resolution of the `/bookmarks/jd` and `/bookmarks/tag` load buttons to properly use `API.list` instead of `API.search` for client-side filtering.
  - Client-side UI enhancements including UTC-to-local timezone conversion in `bookmarks.js` and the implementation of skeleton loading and empty states for the search interface.
  - E2E test suite concurrency refactoring, replacing sequential execution with a dynamic, hardware-aware `Semaphore` scaling algorithm utilizing `multiprocessing.cpu_count()` to massively accelerate Playwright screenshot generation and Lighthouse auditing.
- **Paper Improvements**: Google DeepMind's Antigravity restructured and improved `paper/main.tex` to address rubric gaps, including:
  - The addition of the HSL Color Strategy section.
  - The navigation flow TikZ diagram (Appendix A).
  - The Lighthouse Audit & Visual Regression Reports appendix placeholder.
  - The `console.log` debugging minted example.
  - The `boardman2004stuff` citation integration.
  - The cross-browser SVG `mask-image` and `<picture>` element documentation.
  - The lecture integration parentheticals.
  - Duplicate package declaration cleanup and the updated title page.
- **Backend Architecture Documentation**: Google DeepMind's Antigravity rigorously generated exhaustive, boastful Google-style docstrings across every single backend Python module (spanning `src/models/`, `src/schema/`, `src/security/`, `src/utils/`, `src/middlewares/`, `src/db/`, `src/config/`, `src/api/`, `main.py`, and `scripts/`), including:
  - Comprehensive documentation for EVERY Python class and function to highlight the advanced zero-trust architecture.
  - High-performance `asyncpg` integration and cryptographic resilience with Argon2id/Fernet/JWT.
  - Sub-millisecond route optimization.
  - The initial creation of the `backend_features.md` artifact.
- **Theoretical Foundations Chapter**: Google DeepMind's Antigravity comprehensively generated the drafting and inclusion of the "Theoretical Foundations & Practical Application" chapter in `paper/main.tex`, detailing the rigorous mathematical application of core web systems theories to the DeciMark architecture, specifically:
  - CSS Cascade.
  - Box Model.
  - Typography.
  - Web Media.
