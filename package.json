{
    "scripts": {
        "watch-files": "chokidar './**/*.{html,css,js}' --ignore '.history/**' --ignore 'assets/static/images/screenshots/**' -c \"bash -c 'if [ -z \"$SCREENSHOT_LOCK\" ]; then SCREENSHOT_LOCK=1; node screenshot.js && xelatex -synctex=1 -interaction=nonstopmode -file-line-error -shell-escape main.tex; unset SCREENSHOT_LOCK; fi'\""
    },
    "devDependencies": {
        "chokidar-cli": "^3.0.0",
        "chrome-launcher": "^1.2.1",
        "eslint": "^10.3.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-plugin-prettier": "^5.5.5",
        "htmlhint": "^1.9.2",
        "lighthouse": "^13.0.3",
        "prettier": "^3.8.3",
        "puppeteer": "^24.37.5"
    }
}
